spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/familystory

    username: sa
    password:
    driver-class-name: org.h2.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

  devtools:
    livereload:
      enabled: true

  jpa:
    # show-sql:

    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        # show_sql: true
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: multicampusgroup6@gmail.com
    password: a123456789!
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true


logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace

file:
  path: C:/Users/bae10/Desktop/family_story/project/src/main/resources/static/TeamImage/
